services:
  php-apache:
    build: .
    ports:
      # This links the Host Port to the .env file
      - "${HTTP_PORT}:80" 
    volumes:
      - ./src:/var/www/html

  #db:
    #image: mysql:8.0
    #environment:
      # These link the Database settings to the .env file
      #MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
     # MYSQL_DATABASE: ${MYSQL_DATABASE}